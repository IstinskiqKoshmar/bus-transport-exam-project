@model IReadOnlyList<BusTransport.Core.Models.Trip>
<section class="section">@if (!Model.Any()) { <div class="empty">???? ???????? ???????? ?? ???????? ???????.</div> } else { <div class="result-list">@foreach (var trip in Model) { <article class="result-card"><div><strong>@trip.DepartureCity.CityName ? @trip.ArrivalCity.CityName</strong><span>@trip.Carrier.Name ? @trip.TripCode ? ??????? @trip.BusNumber</span></div><div><b>@trip.DepartureTime.ToString("HH:mm")</b><small>????????</small></div><div><b>@trip.ArrivalTime.ToString("HH:mm")</b><small>??????????</small></div><div><b>@trip.Duration.ToString(@"h\:mm") ?.</b><small>@(trip.HasIntermediateStops ? trip.IntermediateStops : "????????")</small></div><div><b>@trip.Price.ToString("0") ??.</b><small>@trip.AvailableSeats ?????</small></div><a class="primary small" asp-controller="Tickets" asp-action="Book" asp-route-tripId="@trip.Id">??????????</a></article> }</div> }</section>
